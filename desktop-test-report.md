---
created: 2026-09-23
updated: 2026-09-23
---

# DSH Model Router 桌面自动化测试（初测版本 0.3.0）

[已验证] 通过 Harness Dev 原生界面执行 8 轮真实请求，并读取本地会话事件交叉核对。会话 ID：`session-d8f9813c-de3b-471e-a74e-abe61e16bf4a`。本轮未修改路由实现或模型配置，应用最终保持 Auto 模式。

| 用例 | 实际结果 | 判定 |
| --- | --- | --- |
| Auto 翻译“你好” | [已验证] Gemini 返回 Hello，显示“自动选择” | 通过 |
| 同任务翻译“谢谢” | [已验证] 沿用 Gemini，任务起点仍为第 1 轮 | 通过 |
| 复杂协议设计，限百字摘要 | [发现] 仍选择 Gemini，任务起点未更新 | 未达到预期 |
| 手动 Gemini | [已验证] 返回 MANUAL_OK，无自动选择标记，第 4 轮无 Jev 路由事件 | 通过 |
| 切回 Auto，重启应用与 Host | [已验证] 四轮历史与 Auto 状态恢复；翻译“早上好”成功 | 通过 |
| 完整协议反例、修复和证明任务 | [已验证] 升级 Claude，真实请求成功；输出达到 token 上限被截断 | 升级通过，输出完整性未通过 |
| 同任务一句话追问 | [已验证] 保留 Claude，路由 reason 为 uncertain | 行为通过；没有单独证明 sticky 分支 |
| 明确结束旧任务，独立翻译“苹果” | [发现] Claude 返回 Apple，reason 为 session-sticky | 降档未达到预期 |

## 问题与证据

- [发现] 第 3 轮复杂摘要没有升级。使用重建输入额外调用一次 Jev，得到 economyProbability=0.85、newTaskProbability=0.66；这次复查支持分类偏差的解释，但这些不是原始请求的概率，原始概率未保存在会话路由事件中。
- [发现] 第 6 轮成功升级 Claude，第 8 轮明确的新翻译任务仍被 sticky 策略保留在 Claude。任务起点从第 5 至第 8 轮均为第 4 轮，需要进一步验证任务边界识别和任务锚点更新。
- [已确认] 当前 economyThreshold=0.8、newTaskThreshold=0.9；不能只凭这几个样例调低阈值就宣称策略改善。
- [发现] 旗舰配置 maxTokens=2048，完整协议回答出现可见的“已达到输出 token 上限”提示。
- [已验证] 本次 Claude 请求成功，未复现此前地区限制错误。

## 缓存与覆盖范围

[已验证] 第 2 轮后界面累计缓存命中率为 43%，第 5 轮后为 68%，第 6 轮切换模型后为 50%，第 8 轮后为 60%。这些是界面显示的累计指标，不是各轮单独命中率，也不能证明总费用最优。

[限制] 本轮没有通过桌面故障注入覆盖 Jev 超时或错误保留、工具调用过程内不换模型、多模态兼容性。已有自动化测试覆盖部分策略分支，但不能替代这些桌面场景。

[建议] 下一步用固定的中英文多轮样例评估任务边界与复杂度识别，保留判定概率作为诊断信息；再决定提示词、阈值、任务锚点和旗舰输出上限的调整。

## 修复与复测：0.3.4

[已确认] 已安装 0.3.4。默认分类输入改为当前请求和任务起始请求；`historyMessages=0`，历史窗口仍可配置。过滤注入的 user-role 提示；明确短答案不代表低推理难度；切回 Auto 清除旧任务锚点；会话事件持久化判定概率。最终门槛仍为 economy=0.8、new-task=0.9，未保留试验中的 0.75。旗舰输出上限改为 8192，经济型保持 2048。

[已验证] 类型检查、构建及 30 项自动化测试通过；七个固定中英文 Jev 实测用例通过，包括捕获的人工测试会话输入。测试通过仅针对这些样例，不代表普遍分类准确率。

[已验证] 桌面复测会话 `session-9f17fb30-6e4e-43ad-a5a2-c6bf200710fc` 保留中间失败记录，标题中的 0.3.1 是初次建会话时自动生成的标题，并非最终运行版本。第 7 至第 9 轮使用最终 0.3.4：

| 轮次 | 请求 | 实际结果 |
| --- | --- | --- |
| 7 | 独立翻译“苹果” | [已验证] Claude → Gemini；economy=1、new-task=0.97，任务起点更新为 7 |
| 8 | 原百字复杂协议请求 | [已验证] Gemini → Claude；economy=0、new-task=0.96，任务起点更新为 8 |
| 9 | 同任务一句话追问 | [已验证] 保留 Claude；economy=0.87、new-task=0.43，reason=session-sticky，任务起点仍为 8 |

[已验证] 同一会话第 4 轮在提高上限后重测原完整协议问题，输出 3056 tokens，finish=stop，桌面显示正常完成，没有输出上限截断提示。该上限配置在最终版保持不变。

[限制] 默认不向 Jev 发送中间回答细节，依赖这些细节的任务可能需要启用历史窗口并重新评估。回答模型仍接收 Harness 的正常会话历史。本轮验证路由与截断行为，未评审生成协议的技术正确性或答案长度遵循率；8192 仍是有限上限。

[已验证] 第 10 轮使用另一个新任务“翻译明天见”复测，Claude 再次降到 Gemini，返回 See you tomorrow。桌面最终保持 Auto。
